{
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.0",
    "cypress": "^12.17.4",
    "cypress-mochawesome-reporter": "^3.6.0",
    "playwright-webkit": "^1.38.0"
  },
  "scripts": {
    "cypress:info": "npx cypress --version",
    "cypress:run-all-test": "npx cypress run --spec 'cypress/e2e/*.cy.js' --headless --browser chrome",
    "cypress:install-required-plugins": "npm install --save-dev mochawesome mochawesome-merge mochawesome-report-generator"
  }
}
