{
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.0",
    "cypress": "^12.17.4"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^3.5.1"
  },
  "scripts": {
    "cypress:info": "npx cypress --version",
    "cypress:run-all-test": "npx cypress run --spec 'cypress/e2e/*.cy.js' --reporter mochawesome --headless --browser chrome"
  }
}
